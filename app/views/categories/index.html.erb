<p id="notice"><%= notice %></p>

<div class="album py-5 bg-light">
    <div class="container">

      <div class="row">
        <% @categories.each do |category| %>
         <div class="col-md-4">
          <div class="card mb-4 shadow-sm">
            <%= link_to (image_tag category.image_url.url, width: '100%'), category %>
            <div class="card-body">
              <p class="card-text">
                <span><%link_to category.title, categories_path(category)%></span><p><%= category.title %></p><p><%= category.description %></p> 
              </p>
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                  <button type="button" class="btn btn-sm btn-outline-secondary">View</button>
                  <button type="button" class="btn btn-sm btn-outline-secondary">Edit</button>
                </div>
                <small class="text-muted"></small>
              </div>
            </div>
          </div>
         </div>
        <% end %>
      </div>
    </div>
  </div>

<!--<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Title</th>
      <th>Description</th>      
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%# @categories.each do |category| %>
      <tr>
        <td><%#= image_tag category.image %></td>
        <td><%#= category.title %></td>
        <td><%#= category.description %></td>        
        <td><%#= link_to 'Show', category %></td>
        <td><%#= link_to 'Edit', edit_category_path(category) if logged_in?(:site_admin) %></td>
        <td><%#= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' } if logged_in?(:site_admin) %></td>
      </tr>
    <%# end %>
  </tbody>
</table>

<br>-->

<%= link_to 'New Category', new_category_path if logged_in?(:site_admin)%>
