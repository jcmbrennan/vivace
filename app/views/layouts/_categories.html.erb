<% @categories.each do |category| %>
       <div class="lessonholsHome">
           <p><%= category.title %></p>
           <%= link_to (image_tag category.image, :class=>"lessonholsIm"), category %>
           <p><%= category.description %></p>
           <p><a href="/category/<%= category.title %>" class="button"><%= category.title %></a></p>
         </div>
    
    <% end %>
    <%= csrf_meta_tags %>
    